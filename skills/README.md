# Skills

Versioned **agent skills** that ship with runnable assets (scripts, etc.). Each topic lives here **or** under [`../guidelines/`](../guidelines/) as plain markdown — not both.

| Skill | Purpose |
|-------|---------|
| [`github-public-private-workflow/`](github-public-private-workflow/) | Public OSS `upstream` + private `origin`, branch model, optional global block on `github.com` pushes |
